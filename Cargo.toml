[package]
name = "rnop"
version = "0.1.0"
edition = "2024"

[features]
serde = ["dep:serde"]

[dependencies]
serde = {version = "1", optional = true}

[dev-dependencies]
serde = {version = "1", features = ["derive"]}
